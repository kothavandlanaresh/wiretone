WireTone Phase 1.4.1 — Phase 1 finalization

Finalizes the owner-validated Phase 1 protocol work.

Included:
- records successful Windows MSVC 5/5 native test validation
- records successful Android NDK session-library build
- marks the version-1 packet, control, fragmentation, reassembly, and receiver
  session contracts complete
- advances NEXT_ACTIONS to Phase 2.1 Windows WASAPI loopback capture
- hardens the Windows bootstrap with CMake --clean-first so preserved archive
  timestamps cannot silently reuse stale native object files

No protocol semantics, session behavior, Android UI, networking, capture,
playback, jitter-buffer, or codec implementation changes are included.
